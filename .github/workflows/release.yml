on:  
  push:
    tags:
      - '*'

name: Github release

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release -p wordle-gui
      - name: Rename binary
        run: mv target/release/wordle-gui.exe wordle.exe
      - name: Code sign a file with pfx certificate
        uses: DanaBear/code-sign-action@v4
        with:
          certificate: '${{ secrets.CERTIFICATE }}'
          password: '${{ secrets.CERT_PASSWORD }}'
          certificatesha1: '${{ secrets.CERT_HASH }}'
          folder: 'target/release'
      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            LICENSE
            wordle.exe
